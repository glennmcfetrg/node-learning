module.exports.items = ['item1', 'item2', 'item3']
const person = {
  name: 'bob',
}

module.exports.singlePerson = person

// module.exports is a property of the module
// object that we can attach properties to this is why we
// can attach stuff to it
